name: Apply Task 9 feedback metadata GREEN

on:
  push:
    branches:
      - feat/dinov3-multiprototype-remaining
    paths:
      - .github/workflows/task9-apply-feedback-metadata-green.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/dinov3-multiprototype-remaining
      - name: Patch feedback metadata preservation
        run: python .github/scripts/task9_feedback_metadata_green.py
      - name: Commit feedback metadata preservation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/lib/src/screens/species_validation_screen.dart
          git diff --cached --quiet && exit 0
          git commit -m "fix: preserve DINOv3 feedback metadata through filtering"
          git push
