name: Apply Task 9 validation operation GREEN

on:
  push:
    branches:
      - feat/dinov3-multiprototype-remaining
    paths:
      - .github/workflows/task9-apply-validation-operation-green.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/dinov3-multiprototype-remaining
      - name: Patch validation operation id
        run: python .github/scripts/task9_validation_operation_green.py
      - name: Commit validation operation id
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/lib/src/screens/species_validation_screen.dart
          git diff --cached --quiet && exit 0
          git commit -m "feat: generate DINOv3 file validation operation ids"
          git push
