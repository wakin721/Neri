name: Verify Task 9 MainWindow feedback GREEN

on:
  push:
    branches:
      - feat/dinov3-multiprototype-remaining
    paths:
      - .github/workflows/task9-focused-validation-model-path-red.yml

jobs:
  flutter-main-window-feedback:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/dinov3-multiprototype-remaining
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - working-directory: frontend
        run: flutter pub get
      - working-directory: frontend
        run: flutter analyze
      - working-directory: frontend
        run: flutter test test/dinov3_validation_model_path_test.dart test/dinov3_feedback_api_test.dart test/dinov3_box_feedback_ui_test.dart
