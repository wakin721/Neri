# Neri 更新日志

## 3.0.6-alpha3 - 2026-09-07

### 新增

- 新增 NeriCloud 模型同步：应用启动后在后台自动检查模型更新，不阻塞主界面，同时支持在设置中手动“立即同步”。
- 新增“用户模型 / NeriCloud”双来源模型管理。用户自行放入的模型与云端同步模型分别存放、互不覆盖，同名模型可以同时存在。
- 新增模型同步状态界面，可显示检查、下载、完成、失败状态，以及当前文件、下载进度、云端探测/分类模型数量和最近成功同步时间。
- 新增独立模型分发服务，提供版本化 Manifest、短时下载 capability 和代理下载接口。

### 改进

- 本地模型目录统一为小写 `res/model`：
  - `res/model/detect/user`：用户探测模型
  - `res/model/detect/sync`：NeriCloud 探测模型
  - `res/model/cls/user`：用户分类模型
  - `res/model/cls/sync`：NeriCloud 分类模型
  - `res/model/tracker.yaml`：跟踪器配置
- 升级时会自动迁移旧版扁平 `res/model/*.pt`、`res/model_cls/*`，并兼容 alpha2 使用过的 `res/Model` 大写目录；已有用户模型和模型选择尽可能保留。
- 模型同步完成后自动刷新模型列表；正在执行的识别任务不会被热切换模型，更新后的模型从下一次任务开始使用。
- 模型更新以文件大小和 SHA-256 为准，不依赖文件时间戳。

### 稳定性与安全

- 修复旧版训练上传数据库缺少 `event_key` 字段时，启动阶段会在字段迁移前创建索引并触发 `no such column: event_key` 的后端内部错误；现在会先完成兼容迁移，再创建索引，保留现有队列数据。
- 模型下载采用 `.part` 临时文件，完成后校验大小与 SHA-256，再通过原子替换更新正式文件；校验失败不会覆盖已有可用模型。
- 下载优先使用短时 OneDrive / Microsoft HTTPS 直链；直链失败、过期或校验失败时自动回退到 Neri 服务器代理下载。
- 直链与重定向均进行 Microsoft 域名安全校验，服务端凭据不会下发到客户端。
- 代理 capability 绑定单个模型文件并设置过期与使用次数限制，同时增加请求频率、单 IP 每日流量和全局每日流量预算。
- 任一必要模型更新失败时，本轮不会删除旧的 NeriCloud 同步模型，继续保留最后一次可用版本。

### 兼容性说明

- 本次仅将**软件本地模型目录**从 `res/Model` 调整为 `res/model`。
- NeriCloud / OpenList 远端目录仍保持 `NeriCloud/Neri_Data/Model/`，无需调整服务器端模型目录名称。
- `3.0.6-alpha3` 仍为预览版本，建议在正式科研批处理前先验证模型同步和识别结果。
